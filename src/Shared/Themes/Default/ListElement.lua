local Fusion = require(script.Parent.Parent.Parent.Packages.Fusion)
local New = Fusion.New

type ListElementProps = {
    Text: string
}

local function ListElement(props: ListElementProps)
end

return ListElement